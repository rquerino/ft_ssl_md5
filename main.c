 // Learn: bitwise operations, integer overflow, and one-way functions.
// Allowed: open, close, read, write, malloc, free, libft
// usage: ft_ssl command [command opts] [command args]


/*
Pseudocode:

If argc == 1:
    - Function


If argc[1] == md5, sha356 or sha224:



*/
void    ft_errors(int er)
{
    
}

void    ft_hash(char *command, char *opts, char *arg)
{

}

int     main(int ac, char **av)
{
    if (ac == 1)
    {

    }
    else if (ac == 2)
    {

    }
    if (av[1] == "md5" || av[1] == "sha256" || av[1] == "sha224")
        ft_hash(av[1], av[2], av[3]);
    return (0);
}